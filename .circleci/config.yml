version: 2

workflows:
  version: 2

  build_branch:
    jobs:
      - build_base_os
      - build_couchdb
      - push_images

defaults: &defaults
  docker:
    - image: python:3.8
  working_directory: ~/2600hz/kazoo
  shell: /bin/bash --login
  environment:
    BASH_ENV: ".git/.bashrc"
    NETWORK: kazoo
    
jobs:
  build_base_os:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker   
      - run:
        # installs a docker client that will be used to run the docker commands
          name: Install Docker client
          command: |
            set -x
            VER="17.03.0-ce"
            curl -L -o /tmp/docker-$VER.tgz https://get.docker.com/builds/Linux/x86_64/docker-$VER.tgz
            tar -xz -C /tmp -f /tmp/docker-$VER.tgz
            mv /tmp/docker/* /usr/bin      
      - run:
          name: Build BaseOs
          command: |
            cd base-os
            docker build $BUILD_FLAGS -t thecloudpractice/$NETWORK:base-os .
      
      - persist_to_workspace:
          root: .
          paths:
            - .  

  build_couchdb:
    <<: *defaults
    requires:
      - build_base_os
    steps:
      - attach_workspace:
          at: .
      - run:
          name: Build CouchDB
          command: |
            cd couchdb
            docker build $BUILD_FLAGS -t thecloudpractice/$NETWORK:couchdb .          

  push_images:
    <<: *defaults
    requires:
      - build_base_os
      - build_couchdb
    steps:
      - attach_workspace:
          at: .
      - deploy:
          name: Push base image to docker
          command: |
            if ["${CIRCLE_BRANCH}" == "master"]; then
              docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
              docker push thecloudpractice/$NETWORK:base-os
              docker push thecloudpractice/$NETWORK:couchdb
            fi          
